node_modules/
.DS_Store
*.log
.tmp/
# Never commit signing key files in a public repo
*.keystore
signing-key-info.txt
# Local TWA rebuild script contains the keystore password — keep it out of the repo
twa/rebuild.sh
